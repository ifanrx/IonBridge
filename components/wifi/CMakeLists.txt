file(GLOB SRCS_FILES *.cpp)
idf_component_register(
  SRCS
  ${SRCS_FILES}
  INCLUDE_DIRS
  "include"
  REQUIRES
  esp_wifi
  PRIV_REQUIRES
  display
  ble
  controller
  esp_event
  esp_http_client
  logging
  lwip
  machine_info
  mqtt_app
  nvs_data
  utils
  web_server)
