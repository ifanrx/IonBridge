file(GLOB SOURCES *.cpp)
idf_component_register(
  SRCS
  ${SOURCES}
  INCLUDE_DIRS
  "include"
  REQUIRES
  version
  PRIV_REQUIRES
  app_update
  display
  esp_http_client
  esp_https_ota
  esp_partition
  machine_info
  mbedtls
  nvs_data
  storage
  task
  utils)
