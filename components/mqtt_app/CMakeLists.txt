file(GLOB SRCS_FILES *.cpp)
idf_component_register(
  SRCS
  ${SRCS_FILES}
  INCLUDE_DIRS
  "include"
  REQUIRES
  esp_timer
  mqtt
  PRIV_REQUIRES
  app
  ble
  esp_wifi
  machine_info
  mqtt_message
  nvs_data
  task
  utils
  wifi)
